student-form 
student-class-form
major-form
major-class-form
instructor-form
grade-form
class-form
assignment-form


 <div>
    <div class="messages" [ngClass]="(errorMessage)?'alert alert-danger':'alert alert-success'" role="alert" *ngIf="errorMessage || successMessage" id="deletemsg">
        {{errorMessage}} {{successMessage}}
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
  </div>

NOTE:

 ngClass takes a JavaScript object as its value and the object's keys (or any JavaScript identifiers for that matter) must not contain dashes.

 The alert alert-danger and alert alert-success are Bootstrap